$ npm install -g nodemon

El error "nodemon: command not found" indica que 
el comando "nodemon" no está disponible en tu entorno 
de línea de comandos. Esto podría ocurrir si no has
instalado nodemon globalmente o si no se encuentra en 
la variable de entorno PATH.

BACKEND = nodemon server.js

FRONTEND = npm start
